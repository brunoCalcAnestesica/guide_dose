import 'package:flutter/material.dart';

/// Cores do tema do aplicativo.
/// Ajuste conforme a identidade visual do seu app.
class AppColors {
  static const Color primary = Color(0xFF1E223C);
  
  // Cores adicionais que você pode querer usar
  static const Color secondary = Color(0xFF3D5AFE);
  static const Color accent = Color(0xFF00BFA5);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFA726);
  static const Color success = Color(0xFF66BB6A);
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);

  const AppColors._();
}
